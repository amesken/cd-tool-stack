jenkins:
  build: ./jenkins
  ports:
    - "18080:8080"
  links:
    - artifactory:artifactory
    - gitlab:gitlab
    - sonar:sonar
    - selhub:hub
  volumes:
    - ./data_volume/jenkins:/var/jenkins_home:rw